name: Pull Request Check
on:
  pull_request

jobs:
  build:
    name: Build
    runs-on: macos-12
    steps:
    - name: 👀 Checkout repository
      uses: actions/checkout@v1

    - name: 💎 Setup Gems
      run: |
        gem install xcpretty

    - name: 🔨 Set Xcode version
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '13.4.1'

    - name: 🔧 Build
      run: |
        cd Login
        set -o pipefail
        xcodebuild build \
        -scheme Login \
        -destination 'platform=iOS Simulator,name=iPhone 13 Pro' \
        -derivedDataPath build \
        | xcpretty
    
    - name: 📦 Pack derived data artifacts
      uses: actions/upload-artifact@v3
      with:
        name: build
        path: Login/build

  test:
    name: Test
    needs: build
    runs-on: macos-12
    steps:
    - name: 👀 Checkout repository
      uses: actions/checkout@v1

    - name: 📦 Unpack derived data artifacts
      uses: actions/download-artifacts@v3
      with:
        name: build
        path: Login/build

    - name: 💎 Setup Gems
      run: |
        gem install xcpretty

    - name: 🔨 Set Xcode version
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '13.4.1'

    - name: 🧪 Test
      run: |
        cd Login
        set -o pipefail
        xcodebuild test \
        -scheme Login \
        -destination 'platform=iOS Simulator,name=iPhone 13 Pro' \
        -derivedDataPath build \
        | xcpretty